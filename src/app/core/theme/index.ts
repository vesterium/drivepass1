/**
 * Theme barrel export — DrivePass+
 *
 * Usage:
 *   import { colors, spacing, shadows, animation } from '../core/theme';
 */

export { colors } from './colors';
export type { Colors } from './colors';
export { spacing, shadows, animation } from './spacing';
