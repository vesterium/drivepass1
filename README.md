
  # DrivePass+ PWA

  This is a code bundle for DrivePass+ PWA. The original project is available at https://www.figma.com/design/MY6B4PCT1lIy6LjgnjnNcN/DrivePass--PWA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  